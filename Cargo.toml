[package]
name = "projecter"
version = "0.1.0"
edition = "2021"
description = "Terminal project navigator with favorites and workspace launcher"

[[bin]]
name = "projecter"
path = "src/main.rs"

[dependencies]
ratatui = "0.29"

[profile.release]
opt-level = 3
lto = true
strip = true
